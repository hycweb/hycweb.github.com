1.常用的事件
    1.）onclick与ondblclick
        onclick：单击事件
        dbclick: 双击事件
    2.）load与unload
        load事件发生在浏览器加载完页面时，即网页刚被显示出来时。
        unload事件与load事件相对应，发生在离开网页时。
        通常在body标签中设置这两个事件的处理属性。
    3.）mouseover与mouseout
        是鼠标事件。当鼠标移动到HTML元素所在的区域是会触发mouseover事件；
        当鼠标从html元素所在的区域移出时会触发mouseout事件。
    4.）focus与blur
        focus获取焦点事件
        blur失去焦点事件
    ５.)submit事件与reset事件
        都是与表单相关的事件
        submit提交
        reset重置
2.javaScript的内置对象
        String对象：用于处理字符串。
        Math对象：提供数学运算的支持。
        Date对象:用于处理时间和日期。
        Array对象：实现数组的操作功能。

        string对象
            常用的属性
                length：字符串长度
            常用方法
                charAt(index) 返回位于index处的字符。第一个字符的index为0.
                indexOf（substring,[statrtindex]）；从startindex开始向前找，返回第一次出现子字符串substring的位置。如果没有找到子字符串，则返回-1
                substring(start,end) 截取一个字符串。start和end分别是字符串开始和结束的位置。
                toLowercase();将所有字母都转化为小写
                toUppercase()；将所有字母都转化为大写。
        match对象
            常用属性
                PI:3.1415926
                E:e,约为2.31828
            常用的方法
                sin,cos,tan:三角函数
                asin,acos,atan：反三角函数
                abs：绝对值
                Exp(n)e 的n次方
                log（n）自然数
                pow（n1,n2） n1的n2次方
                sqrt（n）平方根
                ceil（n）向上取整
                floor（n）向下取整
                round(n) 四舍五入
                random()0到1之间的随机数
                max（n1,n2） 取大的数
                min（n1,n2） 取小的数
        date对象
            date对象用来处理与时间和日期的相关的操作
            建立Date对象可以采用下面两种方式：
            变量名=new Date（）；
            变量名=new Date(年月日时分秒);
            常用的方法
                getYear()和setYear();获得和设置年的值。
                getMonth()和setMonth（）获得和设置月的值
                getDate()和setDate()获得和设置日期的值。
                getDay、setDay星期的值
                getHours、setHours小时
                getMinutes、setMinutes分钟
                getSeconds、setSeconds秒

        Array对象
            Array对象用来建立数组。
            数组名 = new Array();
            数组名 = new Array(元素个数);
            数组名 = new Array(元素1，元素2......元素N)；









